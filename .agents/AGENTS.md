# Agent Rules

- Automatically run `git add .`, `git commit -m "<message>"`, and `git push origin main` after making any major code changes, so the user doesn't have to manually push the code to trigger deployments.
